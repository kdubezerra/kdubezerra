This directory is where the Visual Studio project files will generate the following items:

- ZIG 2 static-link library binaries:

-- zig2.lib   (release config)
-- zig2d.lib  (debug config)

- Minigame executables

-- minigame.exe   (release config)
-- minigamed.exe  (debug config)
